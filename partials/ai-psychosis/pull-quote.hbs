<style>
  .fullscreen-image-section {
    position: relative;
    width: 100vw;
    height: 100vh;
    margin-left: calc(-50vw + 50%);
    overflow: hidden;
  }

  .fullscreen-image {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
  }

  .pull-quote {
    position: absolute;
    max-width: 400px;
    padding: 2rem;
    background: rgba(4, 13, 18, 0.85);
    backdrop-filter: blur(10px);
    border-left: 4px solid #93B1A6;
    color: #e8e8e8;
    font-size: 1.5rem;
    font-weight: 300;
    line-height: 1.6;
    font-style: italic;
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3);
  }

  .pull-quote.right-aligned {
    top: 50%;
    right: 5%;
    transform: translateY(-50%);
  }

  @media (max-width: 768px) {
    .pull-quote {
      max-width: 90%;
      font-size: 1.2rem;
      padding: 1.5rem;
    }

    .pull-quote.right-aligned {
      right: 50%;
      transform: translate(50%, -50%);
    }
  }
</style>
